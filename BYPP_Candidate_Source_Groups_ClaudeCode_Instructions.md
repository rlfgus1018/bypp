# BYPP — 일정 후보를 추출 출처별로 분류하기

이 문서를 구현 요청으로 취급하라. 현재 코드를 확인한 뒤 일정 후보 검토 화면을 출처별로 나누고, 코드·테스트·문서까지 완성하라. 계획만 작성하고 끝내지 않는다.

## 1. 사용자 요구와 완료 모습

여러 카카오톡 대화 파일에서 추출한 일정 후보가 하나의 목록에 섞여 검토하기 어렵다. 사용자가 어떤 톡방의 파일에서 나온 후보인지 알고, 해당 출처만 골라 검토할 수 있도록 구현한다.

예를 들어 다음 파일에서 추출한 후보는:

```text
PULSE 집행위원회 공지방 31 카카오톡 대화.eml
```

다음 제목 아래에 표시되어야 한다:

```text
PULSE 집행위원회 공지방 31 카카오톡 대화
```

핵심은 **파일의 추출 출처별 그룹 표시와 그룹별 검토**다. 단순히 각 카드에 파일명만 붙이고 끝내지 않는다. 기존 후보도 재업로드·재추출 없이 분류한다.

## 2. 먼저 읽을 것

- `AGENTS.md`, `CLAUDE.md`, `README.md`와 현재 캘린더/Google 연동 문서.
- Next.js 구현 전 `node_modules/next/dist/docs/`에서 App Router의 searchParams, Server Actions, revalidatePath 관련 문서.
- 현재 주요 구현 파일:

| 영역 | 파일 |
|---|---|
| 검토 화면 | `src/app/candidates/page.tsx` |
| 공통 필터 | `src/app/candidates/filters.ts` |
| 개별·일괄 상태 변경 | `src/app/candidates/actions.ts` |
| 일괄 승인·중복 계획 | `src/app/candidates/bulk-plan.ts` |
| 카드·필터·탭·일괄 UI | `src/components/CandidateCard.tsx`, `CandidateFilters.tsx`, `StatusTabs.tsx`, `BulkActions.tsx` |
| 후보·메시지·업로드 데이터 | `src/lib/db/repositories/candidates.ts`, `messages.ts`, `imports.ts` |
| 스키마·DB 초기화 | `src/lib/db/schema.ts`, `client.ts` |
| 업로드 처리·미리보기 | `src/lib/pipeline/ingest.ts` |
| 중복 메시지 판정 | `src/lib/messages/fingerprint.ts` |
| 후보→로컬 일정 생명주기 | `src/lib/calendar/candidate-event-link.ts` |

이 문서보다 현재 코드가 달라졌다면 실제 구조를 기준으로 적용하라. 기존 작업 트리 변경을 보존하고 무관한 코드를 되돌리지 않는다.

## 3. 데이터 연결과 분류 규칙

현재 확인된 출처 연결은 다음과 같다:

```text
schedule_candidates.source_message_id
  → messages.id
  → messages.first_import_id
  → imports.id
  → imports.filename / imports.room_name
```

- 이 경로를 재사용한다. ScheduleCandidate마다 파일명을 복사하거나 LLM에 톡방 분류를 요청하지 않는다.
- 파일 출처는 **메시지를 처음 저장한 업로드** 기준이다. 재업로드한다고 기존 메시지의 최초 출처를 덮어쓰지 않는다.
- 출처 조회는 `LEFT JOIN` 등을 사용해 출처 메타데이터가 없는 기존 후보도 누락시키지 않는다.
- 표시명은 업로드 파일의 basename에서 **마지막 `.eml` 또는 `.txt`만 대소문자 구분 없이 제거**한다. Windows/Unix 경로 문자열 모두에서 디렉터리는 표시하지 않는다.
- 제목 내부의 숫자, 공백, `카카오톡 대화`, 다른 마침표 등은 임의로 제거하지 않는다. 예시의 `31`도 그대로 유지한다.
- 빈 파일명일 때만 `imports.room_name`, `messages.room_name` 순서로 fallback하고, 모두 없으면 `출처 미상`으로 묶는다.
- 원본 파일명은 변경하지 않고 상세 출처 정보에 표시할 수 있다.

### 그룹의 정체성

이번 기능은 카카오톡의 영구 room ID를 도입하는 작업이 아니다. **업로드 파일 기반 출처 그룹**으로 구현하며 아래 규칙을 사용한다.

- 정상 파일명이 있는 경우, 그룹 키의 입력은 `정규화한 파일명 stem + 파싱된 room_name` 조합이다. NFC 및 앞뒤 공백 정리만 하고, 내부 숫자나 날짜를 추측해 제거하지 않는다.
- 같은 파일명·같은 room_name의 재업로드는 같은 그룹으로 표시한다. 매번 생성되는 `imports.id`만으로 그룹을 나누어 같은 출처가 계속 늘어나게 하지 않는다.
- 같은 파일명이지만 파싱된 room_name이 다르면 다른 그룹이다. 제목이 같으면 보조 텍스트로 톡방명을 표시해 구별한다.
- 파일명 fallback 경로와 `출처 미상`에는 충돌하지 않는 별도 key namespace를 사용한다.
- URL 및 React key에는 안정적인 opaque source key를 사용한다. 표시용 문자열을 구분자만으로 결합해 충돌시키지 말고, canonical tuple의 직렬화/해시 등 명확한 방법을 사용한다.
- 파싱된 room_name이 없거나 같은 이름의 서로 다른 실제 톡방은 이 메타데이터만으로 완벽히 구별할 수 없다. 파일명/방 이름이 바뀌면 별도 그룹이 될 수 있다. 자동 병합·이름 변경 UI는 이번 범위 밖이다.
- 그룹 key 생성·표시명 생성은 공통 함수에 모으고 테스트한다. 조회마다 별도 규칙을 만들지 않는다.

## 4. 중요한 기존 중복 판별 한계

현재 `computeFingerprints()`는 시각·발신자·본문·동일 메시지 출현 순서를 사용하고 **톡방을 포함하지 않는다.** 따라서 다른 톡방의 완전히 같은 메시지가 과거 업로드에서 중복 처리되었다면, 최초 출처만 남고 나중 출처는 저장되지 않았을 수 있다.

- 이번 화면 분류는 **DB에 남아 있는 출처**를 정확히 표시한다. 저장되지 않은 과거 출처를 복구했다고 주장하지 않는다.
- 같은 메시지라는 이유로 존재하지 않는 출처 연결을 추측해서 추가하지 않는다. 기존 메시지·후보를 복제하거나 fingerprint를 일괄 재계산하지 않는다.
- 이 한계를 README와 최종 보고에 명시한다. 완전한 다중 톡방 수집 분리는 stable conversation scope와 별도 마이그레이션이 필요한 후속 작업으로 기록한다.
- 이번 요청의 기본 범위를 유지한다. 메시지 dedup 정책 변경, 기존 원문 재처리, 후보 재생성은 포함하지 않는다.

## 5. 화면 동작

`/candidates`에 다음 구성을 구현한다. 기존 Tailwind 스타일과 컴포넌트를 재사용한다.

```text
일정 후보 검토

추출 출처: [전체 출처 ▾]
기존 action / category / 기간 / 정렬 필터
검토 대기(N) | 승인(N) | 무시(N) | 전체(N)

PULSE 집행위원회 공지방 31 카카오톡 대화  · 후보 N건
  [후보 카드]
  [후보 카드]

다른 톡방 카카오톡 대화  · 후보 N건
  [후보 카드]
```

- 기본은 `전체 출처`다. 전체에서도 후보를 **그룹 제목 아래** 나누어 보여준다.
- 드롭다운 또는 접근성 있는 목록으로 출처 하나를 선택하면 그 출처의 후보만 표시한다. 출처가 많을 때 가로 탭이 끝없이 늘어나지 않게 한다.
- 선택 상태는 `source=<sourceKey>` 쿼리 파라미터에 저장한다. 새로고침·직접 링크·뒤로/앞으로 이동에도 유지한다. `source`가 없으면 전체다.
- 출처 선택 시 상태·기존 필터·정렬을 보존한다. 상태 탭 전환과 기간/분류 필터 적용도 출처를 보존한다.
- 기존 필터의 `초기화`는 상태와 선택 출처를 유지하고 검색 조건만 초기화한다. 전체 출처로 돌아가는 동작은 별도로 명확하게 제공한다.
- 선택한 그룹의 검토 대기가 0건이 되어도 선택이 다른 출처로 바뀌면 안 된다. “이 출처에서 현재 조건에 맞는 후보가 없습니다”를 보여준다.
- 필터 때문에 출처 선택지가 사라지지 않게 한다. 선택지는 모든 상태의 후보를 가진 출처를 기준으로 구성하고 현재 선택은 유지한다.
- 긴 한글 파일명은 줄바꿈하고 모바일 가로 overflow를 방지한다. 제목·레이블을 HTML 문자열로 삽입하지 않는다.
- 개별 승인/무시/되돌리기, 원문 보기, 로컬 캘린더 링크, Google 생성 표시를 유지한다.

### 건수·정렬·표시 제한

- 상태 탭 건수는 **선택한 출처 + 나머지 검색 조건** 기준이며, 집계 시 현재 status만 제외하는 기존 규칙을 유지한다.
- 그룹 제목의 후보 수는 해당 status와 검색 조건을 적용한 전체 건수다. 화면에 보이는 수와 다르면 `20 / 150건 표시`처럼 구분한다.
- 후보 카드의 기존 메시지순/일정순 정렬은 그룹 내부에서 유지한다. 동률에는 ID 등 안정적인 tie-breaker를 둔다.
- 그룹 정렬은 이름순 등 하나의 예측 가능한 규칙으로 고정한다.
- 기존 `PAGE_SIZE = 100` 제한과 표시 안내를 유지하거나 합리적으로 개선하되, **전체 목록 100개를 먼저 잘라서 그룹 목록/전체 건수를 계산하지 않는다.**
- 전체 출처 화면에서 일부 그룹의 카드가 제한 때문에 안 보이더라도 출처 선택 목록에는 나타나야 하며, 해당 출처를 선택하면 검토할 수 있어야 한다.
- 메타데이터·집계는 묶어서 조회한다. 카드마다 imports를 조회하는 N+1 구조를 만들지 않는다.

## 6. 필터와 일괄 처리: 가장 중요한 회귀 방지

출처 필터는 화면에서만 적용하면 안 된다. 목록·건수·ID 조회·중복 계획·서버 액션이 같은 조건을 사용해야 한다.

적용 지점:

- `FilterValues`, `readFilters`, `filterFields`, `isFiltering`, `toCandidateFilter`.
- repository의 `listWithSource`, `countByStatus`, `listIds`와 공통 조건 생성.
- `planBulkApproval` 및 `setFilteredCandidatesStatus`.
- 폼 hidden fields, 상태 탭 링크, 성공/실패 redirect와 필터 초기화 링크.

필수 정책:

1. 출처 A를 선택하고 전체 승인/무시/되돌리기를 누르면 **A의 현재 조건에 맞는 후보만** 처리한다. B는 바뀌지 않는다.
2. `전체 출처`에서의 일괄 처리는 기존과 같이 모든 일치 후보를 대상으로 하되, 확인 UI에 `전체 출처`와 실제 대상 건수를 명시한다.
3. 선택 출처에서는 확인 UI에 파일 기반 그룹명과 건수를 표시한다. 화면 100개만 처리하는지 전체 필터 결과인지 모호하게 쓰지 않는다. 기존 정책대로 전체 일치 결과를 처리한다.
4. 서버가 조건을 재검증한다. 숨겨진 form 필드를 신뢰해 임의 SQL을 만들지 않는다. 모든 쿼리는 파라미터 바인딩한다.
5. 존재하지 않거나 잘못된 `source`는 **전체 출처로 조용히 fallback하지 않는다.** 읽기는 오류/빈 목록, 일괄 쓰기는 거부한다. 배열 형태나 위조된 query/form도 동일하다.
6. 기존 대상 건수·중복 건수 변경 감지, 트랜잭션, Google 전송 중 상태 변경 guard를 유지한다.
7. 출처 필터는 후보 선택 범위다. 캘린더 중복 검사는 기존처럼 전체 로컬 캘린더를 기준으로 한다. 다른 출처의 실제 중복 일정을 무조건 무시하지 않는다.
8. 출처 변경 시 이전 일괄 처리 확인 상태를 초기화한다. A에서 열었던 확인 창이 B의 데이터에 적용되지 않게 한다.

## 7. 데이터와 기존 기능 보존

- 기존 `first_import_id`, `filename`, `room_name`을 이용한 조회 확장을 우선한다. 추가 저장이 꼭 필요하면 이유와 마이그레이션을 명시한다.
- 기존 후보 ID·상태·일정 데이터·Google sync row를 변경하지 않고 바로 분류해야 한다.
- 페이지 렌더는 읽기 전용이다. 방문만으로 후보 생성, backfill write, 상태 변경, 외부 API 호출을 하지 않는다.
- candidate 출처 분류 때문에 `CalendarEvent`를 다시 생성하거나 Google로 전송하지 않는다.
- Google sync의 source of truth는 계속 Local CalendarEvent다. Google payload에 톡방명·파일명·원문을 새로 넣지 않는다.
- `.env.local`, 실제 대화 파일, API key, 토큰은 출력·수정·fixture 사용하지 않는다.

## 8. 필수 테스트

가상 데이터와 임시 SQLite를 사용한다. Gemini/Google API 호출은 필요 없으며 테스트에서 차단한다.

1. 예시 `.eml`의 표시명이 정확히 `PULSE 집행위원회 공지방 31 카카오톡 대화`다.
2. `.EML`, `.txt`, 여러 마침표, 한글·특수문자, Windows/Unix 경로, 빈 파일명 fallback.
3. 서로 다른 두 출처의 후보가 다른 그룹에 표시되고 그룹 선택 시 다른 출처가 나오지 않는다.
4. 같은 stem·같은 room_name 재업로드는 같은 그룹, 동일 stem·다른 room_name은 다른 그룹이다.
5. 최초 import 출처를 유지한다. 기존 후보는 재추출 없이 조회로 분류된다.
6. import 또는 방 정보가 없는 후보도 `출처 미상` 등 정의된 fallback으로 보존된다.
7. 출처와 status/action/category/기간/날짜 미확정/정렬의 조합이 목록·건수·ID 조회에서 일치한다.
8. source 파라미터의 URL↔form 변환과 링크·redirect 보존, 초기화 시 출처 유지.
9. 잘못된/없는/배열 source는 전체 쓰기로 확장되지 않는다.
10. A 일괄 승인·무시·되돌리기는 B 후보·로컬 일정·Google sync 데이터를 변경하지 않는다.
11. 출처 변경 시 이전 확인 상태가 초기화되고, 건수 변화 또는 Google sync guard 시 전체 트랜잭션이 보호된다.
12. 100건 이상과 여러 출처에서 그룹 선택지·집계·표시 제한이 일치하고, 화면 밖 출처도 선택 가능하다.
13. 조회 전후 DB 변경이 없고 기존 M1/M2-A/M2-B 테스트가 통과한다.

필요한 핵심 검증을 마친 뒤 다음을 실행한다:

```sh
npm test
npm run typecheck
npm run lint
npm run build
```

브라우저에서 전체 출처 → 특정 출처 → 승인/무시 탭 이동 → 필터 적용/초기화 → 새로고침 → 일괄 처리 범위를 확인한다. 실제 사용자 후보를 검증용으로 대량 변경하지 말고 fixture 환경을 사용한다. 브라우저 확인이 불가능하면 미검증 항목을 명시한다.

## 9. 완료 보고

- 변경 파일과 실제 UI 동작.
- 그룹 key/표시명 규칙, 최초 업로드 출처 정책.
- 기존 후보가 재추출 없이 분류되는 근거.
- 출처별 일괄 처리 범위와 검증 결과.
- 동일 톡방의 이름 변경 및 기존 전역 fingerprint 중복 판별 한계.
- README에 출처 선택·건수·일괄 처리 사용법을 짧게 추가한다.

**완료 기준: 사용자가 일정 후보 탭에서 “PULSE 집행위원회 공지방 31 카카오톡 대화”라는 제목 아래 해당 출처의 후보를 보고, 그 출처만 선택해 다른 출처에 영향을 주지 않고 검토할 수 있다.**
